version: '3.8'
services:
  docker-cleaner:
    build: .
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock # The magic line
    command: ["--dry-run", "--all"] # Default command for safety
